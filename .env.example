# Kanwise dev environment
# Copy this file to .env and adjust values as needed.

# Credentials for `make agent` auto-login (must match an existing account)
KANWISE_EMAIL=you@example.com
KANWISE_PASSWORD=changeme
